<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#1C1B2F"/> <!-- Cor de fundo interna -->
    <stroke android:width="2dp" android:color="@android:color/white" /> <!-- Borda branca -->
    <corners android:radius="12dp"/> <!-- Arredondamento -->
</shape>
